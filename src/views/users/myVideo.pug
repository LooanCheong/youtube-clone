extends ../base
include ../mixins/video

block content
    div 내 동영상
    div.video-grid
        each video in user.videos
            +video(video)
        else
            span.empty__message 비디오를 찾을 수 없습니다